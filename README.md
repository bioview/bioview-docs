# bioview-docs

Documentation for [BioView](https://bioview.readthedocs.io), built with MkDocs Material.

```bash
poetry install
poetry run mkdocs serve
```

Pages live under `bioview-docs/`; the navigation tree is in `mkdocs.yml`.
